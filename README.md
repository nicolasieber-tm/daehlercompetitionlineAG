# dÄHLer Anfrage-Erlebnis (Vorschau)

Klickbare Vorschau des geführten Anfragetools für dÄHLer Competition Line AG. Eine einzelne `index.html`, keine Build-Schritte.

Railway: Repo verbinden, Start-Command wird aus `package.json` gelesen (`npm start`).
